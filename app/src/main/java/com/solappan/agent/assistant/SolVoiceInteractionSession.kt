package com.solappan.agent.assistant

import android.content.Context
import android.service.voice.VoiceInteractionSession

/**
 * System-assistant session shell. P0.4 will add the temporary assistant UI.
 */
class SolVoiceInteractionSession(context: Context) : VoiceInteractionSession(context)
